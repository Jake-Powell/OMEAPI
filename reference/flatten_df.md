# flatten a data frame with (multiple) list elements

flatten a data frame with (multiple) list elements

## Usage

``` r
flatten_df(df)
```

## Arguments

- df:

  data frame

## Value

flat data frame (long format)
